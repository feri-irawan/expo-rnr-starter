import { useMutation } from '@tanstack/react-query';
import { useQueryClient } from '@tanstack/react-query';
import { {{camelCase name}}Service } from '../api/services/{{kebabCase name}}.service';

const use{{pascalCase name}}Mutation = () => {
  const queryClient = useQueryClient();

  return useMutation({ 
    mutationFn: {{camelCase}}Service,
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ["{{camelCase feature}}", "{{camelCase name}}"] })
    }
 });
}

export default use{{pascalCase name}}Mutation